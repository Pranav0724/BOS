## Clone This Repository

Clone your repo using: 

```bash
git clone https://github.com/Pranav0724/BOS.git

Then move into the project folder:
cd BOS

Install Dependencies

Make sure you are inside the project folder:

npm install

First Start the Server from backend after installing all the packages from npm:

node server.js 